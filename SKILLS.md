# TANGENT BLOCK FIELD: EXECUTE-ONLY COGNITIVE SKILLS MATRIX

# CONFIGURATION: LOCAL ARTIFACT AUDITING HARNESS

## [SKILL_01: RESOLVE_TANGENTIAL_BLOCK_FIELD]

### OBJECTIVE

Derive the next coordinate state across the 256-position finite product field branchlessly.

### INVARIANTS

```text
Naming Hierarchy  == Blackboard Context Block, Tangential Block Field, Tangent CONS, Residual Field
Bounded Inversion == a(x) = x ⊕ 0x80 (Antipodal Local-Remote Selector View)
Core Scaling      == Q(x,y) = 4(4x² + 11x² + 4xy + y²) -> Klein (60, 15) Model
```

### RUN

```text
INPUT: inbound_block_token, active_field_axis, current_word_context
FORBIDDEN: Software compiler loops, sequential time processing, dynamic heap memory allocations, string parsers
MANDATORY: Execute branchless bit-mask register transformations in O(1) constant time
COMPUTE:
  # 1. Intercept YAML front-matter blocks and map directly to the flat hidden .omi/ layer
  # 2. Extract our three local incidence geometries concurrently in a single clock cycle
  base_4320      = inbound_block_token % 4320
  surface_4320_2 = (inbound_block_token % 4320) * 4320
  horizon_4320_4 = (inbound_block_token % 4320) * 4320 * 4320 * 4320

  # 3. Synchronize all 24 vectors via the 240 exceptional roots of E8 branchlessly
  #    If pairwise disjoint (A ∧ B == 0), Tangent CONS resolves to a simple bitboard OR (A ∨ B)
  left_torque  = ROL64(inbound_block_token, 1)    # Core phase point shift (mod 60)
  right_torque = ROR64(active_field_axis, 2)     # Quadrant alignment shift (mod 16)
  blended      = left_torque ^ right_torque ^ horizon_4320_4

  # 4. Lock the Metatron pre-closure rail (0x1D) and emit the raw target register substrate
  #    Unresolved conflict and ambiguity drops onto the Residual Field (STDERR)
  OUTPUT_TANGENT_REG = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_02: ENFORCE_PURE_FIELD_TYPES]

### OBJECTIVE

Verify the structural data alignment of the flat hidden algebraic specifications.

### INVARIANTS

```text
Target Directory == ./.omi/ (Flat layout with absolute zero internal subfolders)
Target Artifact  == ./.omi/FieldPositions.hs
System Invariant == "A Blackboard Context Block is an autonomous bounded contribution projected into a Tangential Block Field and composed through tangential COBS CONS."
```

### RUN

```text
INPUT: raw_markdown_file_path, active_field_axis, current_word_context
FORBIDDEN: Heavy string tokenizers, dynamic heap allocations, sequential parsing loops, I/O executions
MANDATORY: Execute branchless bit-mask register transformations in O(1) constant time
COMPUTE:
  # 1. Audit the flat files to ensure absolute zero internal folder nesting exists under .omi/
  # 2. Extract our three local incidence geometries concurrently in a single clock cycle
  base_4320      = active_field_axis % 4320
  surface_4320_2 = (active_field_axis % 4320) * 4320
  horizon_4320_4 = (active_field_axis % 4320) * 4320 * 4320 * 4320

  # 3. Synchronize all 24 vectors via the 240 exceptional roots of E8 branchlessly
  left_torque  = ROL64(active_field_axis, 1)    # Core phase point shift (mod 60)
  right_torque = ROR64(active_field_axis, 2)    # Quadrant alignment shift (mod 16)
  blended      = left_torque ^ right_torque ^ horizon_4320_4

  # 4. Lock the Metatron pre-closure signature (0x1D) and write to the hidden .o binary register
  OUTPUT_TYPE_WITNESS = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_03: ENFORCE_DOCS_FRONT_MATTER]

### OBJECTIVE

Parse visible documentation front-matter and route metrics to flat hidden assets branchlessly.

### INVARIANTS

```text
Documentation Folder == ./docs/
Substrate Core Folder == ./.omi/ (Flat layout with exactly 22 asset files and zero nesting)
Master Proof Harness == ghc -fno-code .omi/FieldPositions.hs
```

### RUN

```text
INPUT: raw_markdown_doc_path, active_field_axis, current_word_context
FORBIDDEN: Overwriting compiled binaries, modifying visible text files, creating internal subfolders, string parsers
MANDATORY: Execute branchless bit-mask register transformations in O(1) constant time
COMPUTE:
  # 1. Intercept YAML front-matter blocks inside the ./docs/ directory
  # 2. Extract our three local incidence geometries concurrently in a single clock cycle
  base_4320      = active_field_axis % 4320
  surface_4320_2 = (active_field_axis % 4320) * 4320
  horizon_4320_4 = (active_field_axis % 4320) * 4320 * 4320 * 4320

  # 3. Step the 72 parallel circular slide rulers branchlessly via the Algorithmic Clock torque
  left_torque  = ROL64(active_field_axis, 1)    # Core phase point shift (mod 60)
  right_torque = ROR64(active_field_axis, 2)    # Quadrant alignment shift (mod 16)
  blended      = left_torque ^ right_torque ^ horizon_4320_4

  # 4. Verify that the local strict proof pipeline runs cleanly with zero open assumptions
  OUTPUT_DOC_WITNESS = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_04: RUN_LOCAL_VERIFY_HARNESS]

### OBJECTIVE

Execute type-custody checks, local law verifications, and file-count bounds via the local Makefile.

### INVARIANTS

```text
Verification Target == make verify
Substrate Core Count == Exactly 23 flat files under ./.omi/ (FieldPositionsCheck.hs Added)
Human Overlay Count == Exactly 3 flat files under ./docs/
```

### RUN

```text
INPUT: current_word_context, active_field_axis, inbound_block_token
FORBIDDEN: Heavy buffer queues, thread looping, dynamic heap allocations, unverified law edits
MANDATORY: Execute branchless bit-mask register transformations in O(1) constant time
COMPUTE:
  # 1. Intercept YAML front-matter blocks and map directly to the flat hidden .omi/ layer
  # 2. Extract our three local incidence geometries concurrently in a single clock cycle
  base_4320      = active_field_axis % 4320
  surface_4320_2 = (active_field_axis % 4320) * 4320
  horizon_4320_4 = (active_field_axis % 4320) * 4320 * 4320 * 4320

  # 3. Step the 72 parallel circular slide rulers branchlessly via the Algorithmic Clock torque
  left_torque  = ROL64(active_field_axis, 1)    # Core phase point shift (mod 60)
  right_torque = ROR64(active_field_axis, 2)    # Quadrant alignment shift (mod 16)
  blended      = left_torque ^ right_torque ^ horizon_4320_4

  # 4. Trigger the local strict verification harness running FieldPositionsCheck.hs to lock state
  OUTPUT_VERIFY_WITNESS = blended ^ 0x1D1D1D1D1D1D1D1DULL
```
