---
omino-centroid: "0x00"
omino-azimuth: "0°"
geometric-stratum: "±0: SYMBOLS"
formal-theory: "Tangential Block Field"
substrate-targets:
  declarations: "./.omi/rules.omi"
  definitions: "./.omi/rules.imo"
  binary: "./.omi/modules.o"
---
# Tangential Block Field

The Tangential Block Field is a category-insulated project layer for the geometric and computational mechanics of shared visibility.

Autonomous knowledge blocks project into a bounded blackboard coordination surface without surrendering their core identities. The visible workspace remains human-readable Markdown with front-matter bindings, while machine-readable `.omi`, `.imo`, and `.o` targets stay flat inside `./.omi/`.

Canonical mapping:

```text
A Blackboard Context Block is an autonomous bounded contribution projected into a Tangential Block Field and composed through tangential COBS CONS.
```

The field is resolved relative to the fixed OMINO Centroid at `0x00` and `0°` across the local `4320`, `4320^2`, and `4320^4` incidence surfaces.
