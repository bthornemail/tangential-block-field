# TANGENT BLOCK FIELD: AUTOMATED DEVELOPER MANDATE

# CLASSIFICATION: FIELD VISIBILITY & COMPOSITION INVARIANT LOCK

# CONFIGURATION STATUS: STANDALONE MAKE HARNESS VERIFICATION LOCK

## 1. INDEPENDENT HARNESS PROTOCOL

You must operate strictly inside this repository context. You are forbidden from linking to global systems or pulling in external make frameworks. All validation is driven exclusively via the local standalone `make verify` target.

## 2. FLAT FILESYSTEM JURISDICTIONS

You are strictly ordered to hide all machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`) flat within the root `/.omi/` directory (expected count: 22). The visible root and the `./docs/` directory (expected count: 3) are reserved for human-readable Markdown files utilizing YAML front-matter references to link to these hidden targets. No internal subfolder nesting is permitted.

## 3. CORE EXECUTABLE INVARIANTS

Every next-state transition must compile branchlessly relative to the OminoCentroid pinch point (`0x00`, `0°`) across the three `4320`, `4320^2`, and `4320^4` local incidence surfaces in absolute O(1) constant time, keeping all output parameters (`accepted=0`, `validated=0`, `receipted=0`) preserved natively in hardware.

The canonical sentence remains locked: A Blackboard Context Block is an autonomous bounded contribution projected into a Tangential Block Field and composed through tangential COBS CONS.
