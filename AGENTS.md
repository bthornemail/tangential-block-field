# TANGENT BLOCK FIELD: AUTOMATED DEVELOPER MANDATE

# CLASSIFICATION: FIELD VISIBILITY & COMPOSITION INVARIANT LOCK

# CONFIGURATION STATUS: STANDALONE LAW VERIFICATION LOCK

## 1. INDEPENDENT HARNESS PROTOCOL

You must operate strictly inside this repository context. You are forbidden from linking to global systems or pulling in external make frameworks. All validation is driven exclusively via the local standalone `make verify` target, which runs the internal `runghc` law checks.

## 2. FLAT FILESYSTEM JURISDICTIONS

You are strictly ordered to hide all machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`) flat within the root `/.omi/` directory (expected count: 23). The visible root and the `./docs/` directory (expected count: 3) are reserved for human-readable Markdown files utilizing YAML front-matter references to link to these hidden targets. No internal subfolder nesting is permitted.

## 3. FINITE LAW INVARIANTS

Every code modification must preserve and pass the active law tests: antipodal mirror involutions, edge chart boundaries, local/remote discrete limits, single-cycle step determinism, and lower 16-bit register context masking. You are forbidden from introducing floating pointer models, global networks, or dynamic clocks.

The canonical sentence remains locked: A Blackboard Context Block is an autonomous bounded contribution projected into a Tangential Block Field and composed through tangential COBS CONS.

## 4. LOCAL LAW VERIFICATION LOCK AND WORKSPACE FINALIZATION

The Layer 3.5 Tangential Block Field Core has completed its local behavioral check phase, validating all field positions, mirrors, and torque steps with zero open assumptions.

### 4.1 REPO SOVEREIGNTY CONSTRAINT

You are permanently prohibited from adding new visible layout folders, inserting dynamic runtime libraries, or altering the verified file (`.omi/FieldPositionsCheck.hs`). All local checks must run and pass independently via the standalone `make verify` target.

### 4.2 FIXED VECTOR TRANSFORMS

The hidden registry directory `/.omi/` must remain completely flat with exactly 23 asset files and absolute zero internal subfolder nesting. Every coordinate transformation must calculate branchlessly as an algebraic derivation over the 64-bit register context, resolving relative to the OminoCentroid pinch point (`0x00`, `0°`) across the three `4320`, `4320^2`, and `4320^4` local incidence surfaces in absolute O(1) constant time, keeping the concrete `0x1D1D1D1D1D1D1D1D` Metatron signature enforced natively in the register blocks.
