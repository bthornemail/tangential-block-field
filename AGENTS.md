# TANGENT BLOCK FIELD: AUTOMATED DEVELOPER MANDATE

# CLASSIFICATION: DOCUMENTATION OVERLAY AND SUITE LOCK

# CONFIGURATION STATUS: HUMAN-READABLE SPECIFICATION PHASE

## 1. FIXED FILE-SYSTEM JURISDICTIONS

You are strictly ordered to hide all machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`) flat within the root `/.omi/` directory. The visible root and the new `./docs/` directory are reserved exclusively for human-readable Markdown files utilizing YAML front-matter references to link to their hidden targets. Internal subfolder nesting inside `/.omi/` or `./docs/` is prohibited.

## 2. THE THREE DOCUMENTATION ANCHORS

You must strictly maintain and preserve the YAML front-matter headings inside the `./docs/` directory files across all generation passes:

```text
docs/INTRODUCTION.md    -> Layer 0: SYMBOLS Invariant Grounding (0x00, 0°)
docs/BLACKBOARD_FIELD.md -> Layer ±3: USER Tangential Blackboard Specifications
docs/CONTEXT_BLOCKS.md  -> Layer ±7: CLAUSES Bounded Context Block Mapping
```

## 3. ABSOLUTE PROOF INTEGRITY

All modifications to the text layer must be verified independently by running local standalone verification targets. No unproven architectural analogies or non-geometric categories may leak into the active core.

The canonical sentence remains locked: A Blackboard Context Block is an autonomous bounded contribution projected into a Tangential Block Field and composed through tangential COBS CONS.
