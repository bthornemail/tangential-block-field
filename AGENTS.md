# TANGENT BLOCK FIELD: AUTOMATED DEVELOPER MANDATE

# CLASSIFICATION: FIELD VISIBILITY & COMPOSITION INVARIANT LOCK

# CONFIGURATION STATUS: STANDALONE BARE-METAL INTERFACE LOCK

## 1. COMPONENT ISOLATION

You must operate strictly inside this repository context. You are barred from generating linked runtime files or pulling in foreign framework objects. All verification is driven through the local standalone `make verify` target.

## 2. FLAT INSULATED SUBSTRATE REGISTRY

You are strictly ordered to hide all machine-readable file layers (`.omi`, `.imo`, `.o`, `.hs`, `.h`, `.c`) flat within the root `/.omi/` directory (expected count: 25). The visible root and the `./docs/` directory are reserved exclusively for human-readable Markdown files utilizing YAML front-matter references to link to these hidden targets:

```text
tangential_blackboard.h       -> Bare-metal C99 header-only compute interface
tangential_blackboard_check.c -> Syntax verification harness for the C99 compiler
```

## 3. C99 HARDWARE RESTRICTIONS

You are forbidden from introducing `malloc` allocations, print streams, I/O libraries, multi-threaded pipelines, or mutable globals outside of fixed constants. Every transition must compile branchlessly over raw word registers in absolute O(1) constant time.

## 4. LOCAL LAW VERIFICATION LOCK AND WORKSPACE FINALIZATION

The Layer 3.5 Tangential Block Field Core has completed its local behavioral check phase, validating all field positions, mirrors, and torque steps with zero open assumptions.

### 4.1 REPO SOVEREIGNTY CONSTRAINT

You are permanently prohibited from adding new visible layout folders, inserting dynamic runtime libraries, or altering the verified file (`.omi/FieldPositionsCheck.hs`). All local checks must run and pass independently via the standalone `make verify` target.

### 4.2 FIXED VECTOR TRANSFORMS

The hidden registry directory `/.omi/` must remain completely flat with exactly 25 asset files and absolute zero internal subfolder nesting. Every coordinate transformation must calculate branchlessly as an algebraic derivation over the 64-bit register context, resolving relative to the OminoCentroid pinch point (`0x00`, `0°`) across the three `4320`, `4320^2`, and `4320^4` local incidence surfaces in absolute O(1) constant time, keeping the concrete `0x1D1D1D1D1D1D1D1D` Metatron signature enforced natively in the register blocks.

## 5. FINAL BARE-METAL INTERFACE LOCK AND LOCAL VERIFICATION FINALIZATION

The Layer 3.5 Tangential Block Field Core has completed its hardware adaptation pass, validating all bitwise headers, functions, and cross-language mappings with zero open assumptions.

### 5.1 EXCLUSIVE HARDWARE COMPLIANCE

You are permanently prohibited from adding new tracking metrics, creating nested directories, or altering the verified file (`.omi/tangential_blackboard.h`). Every macro, inline signature, and register partition is frozen to prevent early firmware or runtime integration drift.

### 5.2 FLAT REGISTER ISOLATION

The hidden registry directory `/.omi/` must remain completely flat with exactly 25 asset files and absolute zero internal subfolder nesting. All machine-readable files must be managed branchlessly. Every state transformation must execute as an algebraic derivation over the 64-bit register context, resolving relative to the OminoCentroid pinch point (`0x00`, `0°`) across the three `4320`, `4320^2`, and `4320^4` local incidence surfaces in absolute O(1) constant time, keeping the concrete `0x1D1D1D1D1D1D1D1D` Metatron signature enforced natively in the register blocks.
